node_modules/
dist/
target/

# napi build artifacts (regenerated by `pnpm build`); keep the hand-written
# native/package.json that marks the generated loader as CommonJS.
native/*
!native/package.json
