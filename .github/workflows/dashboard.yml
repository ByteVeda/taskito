name: Dashboard

on:
  push:
    branches: [master]
    paths:
      - "dashboard/**"
      - ".github/workflows/dashboard.yml"
      - ".github/actions/dashboard-build/**"
  pull_request:
    branches: [master]
    paths:
      - "dashboard/**"
      - ".github/workflows/dashboard.yml"
      - ".github/actions/dashboard-build/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: lint · typecheck · test · build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: dashboard
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/dashboard-build

      - name: Verify routeTree.gen.ts is regenerated
        working-directory: ${{ github.workspace }}
        run: |
          if ! git diff --exit-code --quiet dashboard/src/routeTree.gen.ts; then
            echo "::error::dashboard/src/routeTree.gen.ts is stale. Run 'pnpm --dir dashboard exec tsr generate' and commit the result."
            git --no-pager diff dashboard/src/routeTree.gen.ts
            exit 1
          fi

      - name: Biome (lint + format)
        run: pnpm exec biome ci src/

      - name: TypeScript check
        run: pnpm exec tsc --noEmit

      - name: Unit tests (vitest)
        run: pnpm exec vitest run

      - name: Bundle size summary
        if: always()
        working-directory: ${{ github.workspace }}
        run: |
          set -e
          {
            echo "### Dashboard bundle"
            echo
            echo "| File | Size | Gzipped |"
            echo "|------|-----:|--------:|"
            find sdks/python/taskito/static/dashboard -type f \( -name "*.js" -o -name "*.css" -o -name "*.html" \) | sort | while read -r f; do
              raw=$(stat -c%s "$f")
              gz=$(gzip -c "$f" | wc -c)
              printf "| \`%s\` | %s | %s |\n" "${f#sdks/python/taskito/static/dashboard/}" "$(numfmt --to=iec --suffix=B "$raw")" "$(numfmt --to=iec --suffix=B "$gz")"
            done
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload built assets
        uses: actions/upload-artifact@v7
        with:
          name: dashboard-dist
          path: sdks/python/taskito/static/dashboard/
          retention-days: 7
          if-no-files-found: error
